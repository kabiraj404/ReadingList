javascript:(function(){   try {     alert("1:  Starting GitHub sync");          const GITHUB_USER = "kabiraj404";     const REPO = "ReadingList";     const FILE = "readingList/list.json";     const TOKEN = "ghp_abcdefghijklmnopqrst";           const title = document.title || "";     const url = window.location.href || "";          function getMeta(name) {       const m = document.querySelector('meta[name="'+name+'"], meta[property="'+name+'"]');       return m ? m.content : "";     }          const desc = getMeta("og:description") || getMeta("description") || "";     const img = getMeta("og:image") || "";     const author = getMeta("author");     const siteName = getMeta("og:site_name") || location.hostname;     const personalTag = prompt("Enter your personal word(s):", "");     const date = new Date().toISOString().split("T")[0];          alert("2: Captured metadata + personal input %E2%80%94 now fetching");          const apiUrl = "https://api.github.com/repos/" + GITHUB_USER + "/" + REPO + "/contents/" + FILE;          function fromBase64(b64) {       const binary = atob(b64);       const bytes = new Uint8Array(binary.length);       for (let i = 0; i < binary.length; i++) {         bytes[i] = binary.charCodeAt(i);       }       return new TextDecoder("utf-8", { fatal: false }).decode(bytes);     }          function toBase64(str) {       const bytes = new TextEncoder().encode(str);       let binary = "";       const chunk = 0x8000;       for (let i = 0; i < bytes.length; i += chunk) {         binary += String.fromCharCode. apply(null, bytes.subarray(i, i + chunk));       }       return btoa(binary);     }          fetch(apiUrl, {       headers: {         Authorization: "token " + TOKEN,         Accept: "application/vnd.github.v3+json"       }     })     .then(function(res){       if (!res.ok) {         return res.json().then(function(err) {           throw new Error("Fetch failed: " + (err.message || res.status));         });       }       return res.json();     })     .then(function(data){       if (! data.content) throw new Error("File not found or can't parse");       alert("3: Retrieved list. json");              const old = JSON.parse(fromBase64(data.content));              const newItem = {         title: title,         url: url,         description: desc,         site:  siteName,         author: author,         image: img,         tag: personalTag,         date: date       };              old.unshift(newItem);              const updated = toBase64(JSON.stringify(old, null, 2));              return fetch(apiUrl, {         method: "PUT",         headers: {           Authorization: "token " + TOKEN,           Accept: "application/vnd.github.v3+json",           "Content-Type": "application/json"         },         body: JSON.stringify({           message: "Add " + title,           content: updated,           sha: data.sha         })       });     })     .then(function(r){       if (r.ok) {         alert("4: Successfully updated reading list!");       } else {         r.json().then(function(err){           console.error(err);           alert("Error updating file: " + JSON.stringify(err));         });       }     })     .catch(function(e){       console.error(e);       alert("Error: " + e.message);     });        } catch(e) {     alert("Fatal error: " + e.message);     console.error(e);   } })();
